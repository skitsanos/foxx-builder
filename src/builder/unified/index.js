/**
 * Unified Query Builder for ArangoDB Foxx Services
 * 
 * This module exports a unified query builder that combines the functionality
 * of the previous filter-builder.js and rxq.js implementations.
 *
 * @version 1.0.0
 */

const queryBuilder = require('./query-builder');

module.exports = queryBuilder;
